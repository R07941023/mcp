apiVersion: v1
kind: Service
metadata:
  name: mcp-rag-service
  labels:
    app: mcp-rag-service
spec:
  selector:
    app: mcp-rag-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
