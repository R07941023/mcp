[project]
name = "mcp-web"
version = "0.1.0"
description = "A project using FastMCP for web"
dependencies = [
    "fastmcp>=2.13.2",
    "uvicorn>=0.18.0",
    "tavily>=1.1.0",
    "pyjwt",
    "yfinance",
    "requests",
]
requires-python = ">=3.11"

[tool.uv]
